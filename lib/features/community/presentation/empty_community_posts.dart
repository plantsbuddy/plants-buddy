import 'package:flutter/material.dart';

class EmptyCommunityPosts extends StatelessWidget {
  const EmptyCommunityPosts({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
